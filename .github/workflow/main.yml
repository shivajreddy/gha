name: A workflow for my Hello World file

# the value for 'on' attribute, "triggers" to specify when this workflow runs
on: push

jobs:
  build:
    name: Hello World action
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: ./action-a
      with:
        MY_NAME: "smpl"
