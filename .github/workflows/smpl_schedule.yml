---
name: "scheduled actions"
description: "this runs based on the given schedule"
# author: "smpl"

# workflow_dispatch, shows a manual 'Run Workflow' button that would trigger the event
on: [push, workflow_dispatch]
# on:
  # push:
  #   branches:
  #     - main
  # schedule:
  #   - cron: "*/1 * * * *"
  # workflow_dispatch:

jobs:
  five_job:
    runs-on: ubuntu-latest
    steps:
      - name: Five minute job that shows time
        run: echo "The current server time is $(date)"
